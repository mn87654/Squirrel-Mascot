# Copy this to .env (do NOT commit .env)
BOT_TOKEN=123456:ABCDEF-your-token
ADMINS=123456789,987654321
# Local dev (SQLite). On Render you can replace with your Postgres URL
DATABASE_URL=sqlite+aiosqlite:///./squirrel.db
DAILY_REWARD=100
REFERRAL_REWARD=200
TASK_JOIN_REWARD=100
TIMEZONE=UTC
# Render external hostname (will be provided after deployment on Render)
RENDER_EXTERNAL_HOSTNAME=